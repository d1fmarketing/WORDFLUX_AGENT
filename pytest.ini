[pytest]
testpaths = tests
addopts = -q -ra --strict-markers --disable-warnings --cov=src --cov-report=term-missing --cov-fail-under=65
timeout = 10
asyncio_mode = auto
markers =
    integration: slow/external tests (deselect with -m "not integration")
filterwarnings =
    ignore::DeprecationWarning